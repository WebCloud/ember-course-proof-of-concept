{{#link-to 'index'}}back{{/link-to}}

<form {{action 'signup' on='submit'}}>
  {{input type="text" placeholder="name" value=model.name}}
  {{input type="email" placeholder="email" value=model.email}}
  {{input type="password" placeholder="password" value=model.password}}
  {{input type="password" placeholder="confirm password" value=model.confirmPassword}}
  <button>login</button>
</form>
